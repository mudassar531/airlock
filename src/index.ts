/**
 * Public library entry point. Stable surface for programmatic use.
 * The CLI lives in src/cli.ts and is the primary integration path; this file
 * is for the `wrapTool` SDK helper and policy/audit utilities exposed in v0.1.0+.
 */
export { PRODUCT_NAME, getPackageVersion } from "./constants.js";
